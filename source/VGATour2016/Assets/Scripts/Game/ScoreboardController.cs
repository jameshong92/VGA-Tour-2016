using UnityEngine;
using UnityEngine.UI;
using System.Collections;

public class ScoreboardController : MonoBehaviour {


	private GameObject scoreboard;

	// Use this for initialization
	void Start () {
//		scoreboard = transform.GetChild (0);
	}
	
	// Update is called once per frame
	void Update () {

//
//		if (this.transform.parent.name.Contains ("Temp")) {
//			c.enabled = false;
//		} else {
//			c.enabled = true;
//			text.GetComponent<Text> ().text = GlobalData.players[GlobalData.turn].name + " " + GlobalData.players [GlobalData.turn].shotCount;
//		}
	}
}
